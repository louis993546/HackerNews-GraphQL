type Story {
    id: ID!
    # by: User!
    score: Int!
    url: String!
    title: String!
    time: Timestamp!
    # comments: [Comment!]!
    commentCount: Int!
}

scalar Time

type Timestamp {
    iso8601: Time!
    unixTime: Int!
}

type Query {
    story(id: ID!): Story!
}